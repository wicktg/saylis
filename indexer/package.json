{
  "name": "saylis-indexer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-curve-addresses.mjs && ponder dev",
    "start": "node scripts/generate-curve-addresses.mjs && ponder start",
    "db": "ponder db",
    "codegen": "ponder codegen"
  },
  "dependencies": {
    "ponder": "^0.11.0",
    "viem": "^2.21.0",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
